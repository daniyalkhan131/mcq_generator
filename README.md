 1- setup env
 2- experiments in jupyter notebook
 3-  modular
 4- web api steamlit
 5- deploymeny

 conda create -p mcq_gen python==3.10 -y

 use setup.py to install libraries packages defined in requirement in enviorment
 and to install local package into our current enviorment we directly install setup.py file- python setup.py install, it will install package in current folder into the current env

 and if we write -e . in requirements.txt file, it will search all local package in our current folder and install in virtual env
 
